openapi: 3.1.1
info:
  title: Legacy Closing Commands API
  version: 0.1.0
  description: >
    Legacy / superseded contract retained for traceability only. The canonical V1 contracts live in
    `closing-folders-api.yaml`, `import-balance-api.yaml`, `manual-mapping-api.yaml` and
    `financial-summary-api.yaml`.
paths:
  /api/closings/{closingId}/imports/balance:
    post:
      operationId: createBalanceImport
      summary: Upload a balance import file
      parameters:
        - in: path
          name: closingId
          required: true
          schema: { type: string }
      responses:
        "202":
          description: Import accepted
  /api/closings/{closingId}/mapping:
    put:
      operationId: replaceMappings
      summary: Replace or upsert mappings for a closing
      parameters:
        - in: path
          name: closingId
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [mappings]
              properties:
                mappings:
                  type: array
                  items:
                    type: object
                    required: [accountId, targetRubricId]
                    properties:
                      accountId: { type: string }
                      targetRubricId: { type: string }
      responses:
        "200":
          description: Mappings updated
  /api/closings/{closingId}/financials:
    get:
      deprecated: true
      operationId: getFinancials
      summary: Legacy stub superseded by closing-folders financial summary
      description: >
        Superseded by GET /api/closing-folders/{closingFolderId}/financial-summary.
        Retained here only as a legacy stub and not as the canonical contract.
      parameters:
        - in: path
          name: closingId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Legacy stub, superseded by financial-summary-api.yaml
