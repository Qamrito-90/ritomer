# Internal rehearsal observation template V1

Classification: `INTERNAL_ONLY / EMPTY_TEMPLATE`.

Never commit a completed copy of this template. Do not enter an identity, participant quote, screenshot, HAR, token, credential, local path, real data or customer data.

## Observation unit

- `runId`:
- `taskId`:
- `actorRole`:

## Timestamps

- `startedAtUtc`:
- `endedAtUtc`:

## Result

- `result`:

Allowed result vocabulary when a separately authorized internal rehearsal is recorded outside Git: `COMPLETED`, `COMPLETED_WITH_HELP`, `NOT_COMPLETED`, `NOT_ATTEMPTED`.

## Time accounting

- `productiveSeconds`:
- `incidentSeconds`:
- `excludedSeconds`:

No target or threshold is defined by this template.

## Assistance and recovery

- `interventions`:
- `blockers`:
- `workarounds`:
- `corrections`:

Allowed intervention categories: `PROTOCOL_CLARIFICATION`, `NAVIGATION_HINT`, `DOMAIN_EXPLANATION`, `TECHNICAL_RECOVERY`, `DATA_RESET`, `SESSION_AUTH_RECOVERY`.

## Understanding and handoff

- `comprehension`:
- `handoff`:

## Usefulness and irritants

- `utility`:
- `irritants`:

This Git artifact is a field definition only. Observation values belong to a future Security/Privacy-approved location outside Git.
